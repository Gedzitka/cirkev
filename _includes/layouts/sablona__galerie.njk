
{% include "partials/header.njk" %} 
{% include "partials/menu.njk" %}
<section class="gallery__photo">
    <div class="gallery__name">
        <h1>{{ title }}</h1>
      </div>
      
      <div class="gallery__grid">
        <img class="grid__photo" src="{{ photo }}" alt="{{ alt }}">
        <img class="grid__photo" src="{{ photo2 }}" alt="{{ alt2 }}">
        <img class="grid__photo" src="{{ photo3 }}" alt="{{ alt3 }}">
        <img class="grid__photo" src="{{ photo4 }}" alt="{{ alt4 }}">
        <img class="grid__photo" src="{{ photo5 }}" alt="{{ alt5 }}">
        <img class="grid__photo" src="{{ photo6 }}" alt="{{ alt6 }}">
        <img class="grid__photo" src="{{ photo7 }}" alt="{{ alt7 }}">
      </div>
      
    </section>

{% include "partials/contact.njk" %}
{% include "partials/footer.njk" %} 
 <script src="/js/script.js"></script>  
 <script>
  if (window.netlifyIdentity) {
    window.netlifyIdentity.on("init", user => {
      if (!user) {
        window.netlifyIdentity.on("login", () => {
          document.location.href = "/admin/";
        });
      }
    });
  }
</script> 
</body>
</html>